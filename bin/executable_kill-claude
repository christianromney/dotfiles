#!/usr/bin/env fish
# kill-claude: force-kill all Claude Code processes

set pids (pgrep -x claude 2>/dev/null)

if test -z "$pids"
    echo "No claude process found."
    exit 0
end

echo "Killing claude PIDs: $pids"
kill -9 $pids
echo "Done."
